# Ghost Content API (build-time). Absent locally => the blog builds from fixtures.
# A production build with these missing fails loudly rather than deploying an empty blog.
GHOST_CONTENT_API_URL=https://cms.everyware.in
GHOST_CONTENT_API_KEY=

# Canonical origin used for canonical URLs, sitemap and OpenGraph.
PUBLIC_SITE_URL=https://everyware.in
